name: TiMP_based
services:
  server:
    build: .
    ports:
      - "38100:44444"
    volumes:
      - db_data:/app/data
    environment:
      - DB_PATH=/app/data/timp.db
    restart: unless-stopped

volumes:
  db_data:
